{
  "name": "plaid-pattern",
  "version": "1.0.7",
  "private": true,
  "description": "Plaid Pattern - Personal Finance Manager sample app",
  "scripts": {
    "install:all": "npm install --prefix client && npm install --prefix server",
    "client": "npm start --prefix client",
    "server": "npm start --prefix server",
    "db:create": "docker compose up -d --wait postgres",
    "db:down": "docker compose down",
    "db:logs": "docker compose logs -f postgres",
    "db:shell": "docker compose exec postgres sh -c 'psql -U \"$POSTGRES_USER\" -d \"$POSTGRES_DB\"'",
    "db:reset": "docker compose down -v && docker compose up -d --wait postgres"
  }
}
